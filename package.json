{
  "name": "c-projects-continuity-os-04-product-shells-crypto-guides-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "generate:build-meta": "node scripts/generate-build-meta.mjs",
    "generate:discovery": "node scripts/generate-discovery-v2.mjs",
    "build:site": "npm run generate:build-meta && npm run generate:discovery && astro build",
    "verify:public": "node scripts/verify-public-contract.mjs",
    "build": "npm run build:site && npm run verify:public",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.4"
  }
}
